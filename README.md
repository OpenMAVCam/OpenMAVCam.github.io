# OpenMAVCam Website

The English documentation and product website for OpenMAVCam.

```bash
npm ci
npm run start
npm run validate
```

To publish, enable **Settings → Pages → Source → GitHub Actions** in `OpenMAVCam/OpenMAVCam.github.io`. Pull requests validate the static site; pushes to `main` deploy it.
